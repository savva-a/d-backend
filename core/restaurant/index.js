const save = require('./save');
const followersView = require('./followersView');
const view = require('./view');
const list = require('./list');

module.exports = {
  save,
  view,
  followersView,
  list
};
